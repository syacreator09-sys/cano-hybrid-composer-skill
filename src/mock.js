import {mkdir,writeFile} from 'node:fs/promises';import path from 'node:path';import {buildRenderPlan} from './render-plan.js';
export async function renderMock(timeline,outDir){await mkdir(outDir,{recursive:true});const plan=buildRenderPlan(timeline);const manifest={version:'1.0',projectId:timeline.projectId,mode:'mock',status:'PLANNED',timeline:'timeline.json',renderPlan:'render-plan.json',output:null};await writeFile(path.join(outDir,'timeline.json'),JSON.stringify(timeline,null,2));await writeFile(path.join(outDir,'render-plan.json'),JSON.stringify(plan,null,2));await writeFile(path.join(outDir,'composer-manifest.json'),JSON.stringify(manifest,null,2));return manifest;}
